# Dependency governance record for releaser
# Each crate is explicitly approved for use in this project.
# Approved 2026-04-06

[crates.clap]
version = "4.6.0"
reason = "CLI argument parsing — core to the product"

[crates.serde]
version = "1.0.228"
reason = "Serialization/deserialization for config and API types"

[crates.toml]
version = "1.1.2"
reason = "TOML parsing for releaser.toml manifest"

[crates.thiserror]
version = "2.0.18"
reason = "Derive macro for error types"

[crates.cargo_toml]
version = "0.22.3"
reason = "Parse Cargo.toml to derive config defaults"

[crates.semver]
version = "1.0.28"
reason = "Semantic version parsing and comparison"

[crates.url]
version = "2.5.8"
reason = "URL parsing for repository field validation"

[crates.assert_cmd]
version = "2.2.0"
reason = "CLI integration testing (dev-dependency)"
dev = true

[crates.predicates]
version = "3.1.4"
reason = "Test assertions for CLI output (dev-dependency)"
dev = true

[crates.tempfile]
version = "3.27.0"
reason = "Temporary directories for hermetic tests (dev-dependency)"
dev = true

[crates.insta]
version = "1.47.2"
reason = "Snapshot testing for generated templates (dev-dependency)"
dev = true
